# K Group 명함 자동발주 시스템 개발·운영 지침서

문서 기준일: 2026-07-27 · 대상: 운영 담당자, 관리자, 인수인계 담당자

> 이 문서는 현재 프로그램과 설정 파일을 기준으로 한 운영 안내입니다. 이 문서 작성 중 메일 발송, 발송 모드, 계정, 수신자/참조자 설정은 변경하지 않았습니다.

## 1. 문서 목적과 시스템 도입 배경

직원이 메일로 제출한 명함 신청서를 수집하고, 업체 발주 메일 초안을 만들며, 담당자의 확인 또는 설정된 자동 발송으로 처리하는 도구입니다. 반복 입력과 누락을 줄이되, 외부 발송의 책임은 운영 담당자에게 남습니다.

## 2. 시스템 한눈에 보기

현재 운영 포털은 이 PC에서만 열리는 `http://127.0.0.1:8765/`입니다. 설정된 감시 메일함에서 신청서를 읽고, 포털에서 초안, 발송 가능 건, 확인 필요 건, 대시보드를 확인합니다. 실제 메일함 주소와 업체 수신 주소는 이 문서에 기록하지 않습니다.

## 3. 전체 업무 흐름

```text
감시 메일함 확인 (기본 60초 간격, 최근 30건)
  → 제목의 `명함` 검사 및 Excel 형식 검사
  → inbox/requests/에 원본 저장
  → 양식·필수값 검증 및 발주 초안 생성
  → 직접 승인: 담당자 최종 확인 후 발송
     자동 발송: 새로 들어온 검증 완료 건만 발송
  → 결과·이력·해시를 JSON에 저장하고 대시보드에서 확인
```

## 4. 대시보드와 운영포털의 역할

**대시보드는 읽기 전용 확인 화면**입니다. 처리 상태, 우선 확인할 항목, 최근 수집·발송 이력을 보고 운영 순서를 판단할 때 사용하며, 대시보드에서 수신자·문구·모드나 발송 상태를 변경하지 않습니다.

**운영 포털은 상태를 변경하고 발송을 실행하는 화면**입니다. 업체 수신·참조 주소와 기본 문구, 초안별 수정값, 직접 승인/자동 발송 모드를 바꾸고, 미리보기 확인과 최종 발송을 수행합니다. 조회 화면과 변경·발송 화면을 분리하면 현황을 살피는 중 실수로 운영 상태를 바꾸는 위험을 줄일 수 있습니다.

## 5. 사용 기술과 선택 이유

| 기술 | 역할 | 선택 이유 |
|---|---|---|
| Python | 수집, 검증, 초안, 발송, 포털 실행 | 단일 실행 환경에서 업무 규칙을 관리 |
| openpyxl | Excel 신청서 읽기 | `.xlsx`/`.xlsm` 양식의 헤더와 값을 확인 |
| python-dotenv | 비밀 설정 로드 | 계정·인증 정보를 코드와 분리 |
| 사내 메일 API | 메일 조회·첨부 다운로드·발송 | 사내 메일함과 연동 |
| HTTP/HTML/CSS/JavaScript | 로컬 포털·미리보기·대시보드 | 별도 웹서버 설치 없이 운영 화면 제공 |
| JSON | 설정과 처리 이력 저장 | 사람이 열어 확인 가능한 파일 기반 저장 |
| BAT | 더블클릭 실행 | 비개발자 실행 편의 |
| unittest | 자동 점검 | 실제 외부 메일 없이 주요 안전 규칙 확인 |

## 6. 구성 요소와 파일별 역할

| 파일/구성 | 책임 |
|---|---|
| `business_card_mailer.py` | 메일 수집, 첨부 저장, Excel 검증, 초안·CLI 처리 |
| `business_card_portal.py` | 로컬 운영 포털과 승인 요청 처리 |
| `business_card_automation.py` | 직접 승인/자동 발송 모드와 자동 주기 처리 |
| `business_card_sending.py` | 발송 직전 수신자·원본·첨부·중복 검증 |
| `business_card_storage.py` | JSON 안전 저장, 복구, 작업 잠금 |
| `business_card_mail_editor.py` | 업체 수신/참조·제목·문구와 초안별 수정값 관리 |
| `business_card_dashboard.py` | 처리·발송 결과 요약 화면 |
| `recover_business_card_state.py` | 손상된 상태 파일 복구 |
| `run_business_card_mailer.bat` | 운영용 시작 파일 |

## 7. 메일 수집, 양식 검증, 초안 생성과 발송 과정

접수 대상은 메일 **제목에만** 정확한 부분문자열 `명함`이 들어 있는 경우입니다. 첨부 파일명은 대상 선정에 사용하지 않습니다. 제목에 `명함`이 없으면 파일명이 명함신청서여도 대상이 아닙니다. 허용 발신자 목록이 비어 있으면 제목·첨부 형식 조건을 통과한 발신자를 모두 접수할 수 있으므로, 운영 환경에서는 내부 발신자만 허용하도록 설정하는 것을 권장합니다. 지원 첨부 형식은 `.xlsx` 및 `.xlsm`이며, 구형 .xls 형식은 지원하지 않습니다.

1. 시스템이 감시 메일함에서 최근 메일을 확인합니다.
2. 제목에 `명함`이 있는지, 첨부가 `.xlsx` 또는 `.xlsm`인지 확인합니다.
3. 허용 발신자 목록이 설정되어 있으면 그 목록에 있는 발신자만 계속 처리합니다.
4. 원본 Excel을 `inbox/requests/`에 저장하고, 이름 헤더와 최소 헤더 매핑 조건·필수 필드를 점검합니다.
5. 통과한 신청서에서 발주 메일 초안과 원본 첨부 정보를 만듭니다. 문제가 있으면 자동 발송 대상이 아니라 ‘확인 필요’로 남습니다.
6. 발송 직전에 수신자, 원본 파일 해시, 첨부 존재 여부와 이미 발송한 해시를 다시 확인합니다.
7. 성공 건은 즉시 이력에 저장합니다. 여러 건 중 일부가 실패해도 성공한 건을 다시 보내지 않도록 합니다.

## 8. 데이터 관리 방식과 DB 사용 여부

이 시스템은 **별도 데이터베이스를 사용하지 않습니다**. JSON 파일과 원본 Excel 파일을 파일 기반 저장소로 사용합니다. JSON은 프로그램이 읽고 쓰는 표준 텍스트 형식입니다.

| 위치 | 내용 | 운영 주의 |
|---|---|---|
| `inbox/automation_settings.json` | 발송 모드 | 현재 값 확인 후에만 변경 |
| `inbox/vendor_mail_template.json` | 업체 To/CC, 제목·문구 기본값 | 실제 수신자 값은 공유 문서에 적지 않음 |
| `inbox/request_sheet_mapping.json` | 신청서 헤더 매핑·필수 항목 | 양식 변경과 함께 검토 |
| `inbox/requests/` | 저장된 원본 신청서 | 개인정보 포함 가능 |
| `output/processed_state.json` | 수집 UID, 발송 해시, 이력 | 삭제·초기화 금지 |
| `output/business_card_drafts.json` | 현재 발주 초안 | 포털 미리보기와 함께 확인 |
| `output/business_card_mail_fetch_result.json` | 최근 수집 결과 | 오류 원인 확인 |
| `output/business_card_send_result.json` | 최근 발송 결과 | 발송 성공·실패 확인 |
| `output/backups/` | 복구 전 원본 백업 | 복구 확인 전 보존 |
| `output/business_card_operation.lock` | 동시 실행 방지 표식 | 강제 삭제 전 실행 중 작업 확인 |

**원자적 저장(atomic save)** 은 새 JSON을 임시 파일에 완성한 뒤 기존 파일과 교체하는 방식입니다. 저장 도중 전원이 꺼져도 반쯤 쓴 파일이 남을 위험을 낮춥니다. **작업 잠금(operation lock)** 은 수집·초안 생성·발송을 동시에 실행하지 못하게 하는 표식입니다.

## 9. 메일함과 업체 수신·참조 주소와 문구 설정

감시 메일함 관련 설정은 `.env`의 메일함·검색·간격 항목입니다. 업체 발주 To/CC와 기본 문구는 포털의 기본 발송 설정 또는 `inbox/vendor_mail_template.json`에서 다룹니다. 초안별로 이번 발송에만 적용할 수정값도 별도로 저장될 수 있습니다.

운영 시에는 주소를 값 그대로 채팅, 보고서, 화면 캡처에 넣지 마십시오. 주소 변경 전에는 수신(To), 참조(CC), 제목, 본문, 원본 첨부를 미리보기에서 함께 확인합니다.

## 10. 직접 승인과 자동 발송 모드

| 구분 | 직접 승인 | 자동 발송 |
|---|---|---|
| 기본 상태 | 기본/안전 모드 | 명시적으로 전환해야 함 |
| 발송 시점 | 담당자가 최종 확인 후 실행 | 새로 수집되어 검증 완료된 건을 즉시 처리 |
| 적합한 상황 | 초기 운영, 양식·수신자 변경 직후 | 검증된 반복 운영 |
| 담당자 확인 | 매 발송 전 필수 | 전환 전 설정·문구·수신자 검토 필수 |
| 위험 | 처리 지연 가능 | 잘못된 설정이면 외부 발송이 즉시 일어날 수 있음 |

자동 발송은 기존에 있던 모든 초안이 아니라, 해당 주기에 새로 들어와 검증을 통과한 준비 완료 건만 선택합니다. 필요하지 않으면 즉시 직접 승인으로 되돌리십시오.

## 11. 일상 운영 순서와 체크리스트

- [ ] `run_business_card_mailer.bat` 또는 포털이 정상 실행되었는지 확인한다.
- [ ] 포털에서 현재 모드가 직접 승인인지 자동 발송인지 확인한다.
- [ ] 새 신청서 수, 발송 가능 건, 확인 필요 건을 확인한다.
- [ ] 미리보기에서 수신/참조, 제목, 본문, 신청 인원, 원본 첨부를 확인한다.
- [ ] 직접 승인 모드에서는 최종 발송 버튼을 누르기 전에 한 번 더 확인한다.
- [ ] 발송 후 대시보드와 `output/business_card_send_result.json`에서 결과를 확인한다.
- [ ] 오류·확인 필요 건은 원인과 조치 내용을 남긴다.

## 12. 오류 대응: 유형과 조치 방법

| 증상 | 가능한 원인 | 조치 |
|---|---|---|
| 포털이 열리지 않음 | 포털 미실행, 이미 종료됨, 로컬 주소·포트 문제 | `run_business_card_mailer.bat` 실행 여부와 포털 로그를 확인하고, 담당자에게 알린다. 포털을 중복 실행하거나 설정 파일을 임의로 바꾸지 않는다 |
| 새 메일이 보이지 않음 | 포털/모니터 미실행, 메일함·네트워크 문제 | 실행 상태와 감시 메일함 설정을 확인하고 결과 파일을 본다 |
| 신청서가 제외됨 | 제목에 `명함` 없음, 형식 불일치, 허용 발신자 불일치 | 제목·발신자·확장자를 확인한다 |
| 파일이 접수되지 않음 | `.xls`, 비Excel, 다운로드 오류 | `.xlsx` 또는 `.xlsm`으로 다시 제출받는다 |
| 확인 필요 상태 | 필수 헤더·필수 값 부족, 양식 매핑 불일치 | 원본과 `request_sheet_mapping.json`을 대조한다 |
| 발송이 거절됨 | 수신자/첨부/원본 해시 변경 또는 중복 발송 | 미리보기와 원본 파일을 재확인하고 새 승인 절차를 밟는다 |
| 일부 초안만 발송되고 나머지는 실패함 | 여러 건 중 일부 검증·메일 전송 실패 | 발송 결과에서 성공·실패 건을 구분한다. 성공한 해시는 다시 발송하지 말고, 미해결 건만 정상 미리보기·검토·승인 경로로 재시도한다 |
| 자동 발송을 즉시 중지해야 함 | 수신자·문구 오류 또는 비정상 반복 위험 | 포털을 열어 즉시 직접 승인 모드로 전환한다. 포털을 열 수 없으면 포털 프로세스를 종료하고 담당 개발자에게 에스컬레이션하며, 상태 JSON을 직접 편집하지 않는다 |
| 다른 작업이 진행 중이라는 메시지 | 작업 잠금 존재 | 다른 포털·명령 실행을 종료한 뒤 재시도한다 |
| JSON 읽기 오류 | 상태 파일 손상 | 아래 복구 절차를 따른다 |

## 13. 백업과 복구

1. 포털과 모니터를 먼저 종료합니다.
2. `py -B recover_business_card_state.py`를 실행합니다. 이 명령의 복구 범위는 `output/business_card_mail_fetch_result.json`과 `output/processed_state.json` 두 파일뿐입니다.
3. `output/backups/<timestamp>/`에 만들어진 손상 전 원본 백업을 확인하고, 복구 확인이 끝날 때까지 보존합니다.
4. 포털을 다시 시작한 뒤 최근 수집 결과, 현재 초안, 처리 이력과 중복 방지 해시가 예상대로 보이는지 확인합니다.

초안(`business_card_drafts.json`), 자동화 설정, 업체 메일 템플릿, 신청서 매핑 또는 그 밖의 JSON 손상은 이 명령으로 복구되지 않습니다. 이런 파일이 의심되면 포털을 중지하고, 영향받은 파일을 그대로 보존·복사한 뒤, 확실하지 않은 JSON을 손으로 편집하지 말고 담당 개발자에게 에스컬레이션합니다. `processed_state.json`을 삭제하거나 빈 파일로 바꾸지 마십시오. 발송 해시가 사라지면 과거 신청서가 다시 발송 대상으로 보일 수 있습니다.

## 14. 개인정보와 보안 주의사항

신청서, 초안, 결과 JSON과 백업에는 이름, 연락처, 이메일 등 개인정보가 포함될 수 있습니다. 승인된 담당자만 폴더에 접근하게 하고, 조직의 보관·파기 정책에 따라 정리합니다. 이 프로그램은 개인정보를 자동 삭제하지 않습니다.

`.env`는 계정 인증 정보를 담으므로 공유 저장소, 메신저, 보고서, 화면 캡처에 포함하지 마십시오. 포털은 `127.0.0.1`에만 열립니다. **CSRF**는 다른 웹페이지가 담당자 모르게 발송 요청을 보내는 것을 막는 확인 표식이고, 포털은 이를 검사합니다. **초안 digest**는 승인 화면을 연 뒤 초안이 바뀌지 않았는지 확인하는 요약 지문입니다. 둘 중 하나가 맞지 않으면 다시 검토해야 합니다.

**해시(hash)** 는 파일 내용에서 계산한 짧은 지문입니다. 원본이 바뀌었는지와 같은 신청서를 이미 발송했는지를 확인하는 데 사용합니다. 해시 자체를 비밀번호처럼 취급할 필요는 없지만, 원본 파일과 이력은 보호해야 합니다.

## 15. 변경 시 주의할 설정과 파일

| 변경 대상 | 영향 | 변경 전 확인 |
|---|---|---|
| 메일함·간격·허용 발신자 설정 | 수집 범위와 빈도 변경 | 감시 메일함과 제목 조건을 재확인 |
| `vendor_mail_template.json` | 모든 기본 수신/참조·문구에 영향 | 실제 외부 발송 대상과 제목을 미리보기로 확인 |
| `request_sheet_mapping.json` | Excel 인식과 필수값 판정에 영향 | 실제 신청서 사본으로 테스트 |
| `automation_settings.json` | 직접 승인/자동 발송 전환 | 자동 발송의 외부 발송 위험을 승인 |
| `.env` | 인증·메일 연동·운영 설정에 영향 | 비밀값 노출 없이 담당자 절차에 따라 변경 |
| `output/processed_state.json` | 중복 방지·이력에 영향 | 임의 삭제 대신 복구 절차 사용 |

코드나 설정을 바꾼 뒤에는 실제 업체로 보내지 말고, 테스트와 포털 미리보기로 먼저 확인합니다.

## 16. 테스트와 운영 확인 방식

전체 자동 점검은 다음 명령으로 실행합니다.

```powershell
py -B -m unittest discover -s tests -v
```

테스트는 가짜 메일 클라이언트나 임시 파일을 사용하며 실제 업체 메일을 발송하지 않습니다. 운영 확인은 포털에서 현재 모드, 최근 수집 결과, 준비된 초안, 발송 결과를 순서대로 보는 방식으로 합니다. 수동 점검 때도 발송 버튼을 누르기 전에는 미리보기 확인이 우선입니다.

## 17. 자주 묻는 질문

**Q. 파일명에 명함이 있는데 왜 제외되었나요?**  
A. 파일명이 아니라 메일 제목만 검사합니다. 제목에 정확한 부분문자열 `명함`이 있어야 합니다.

**Q. `.xls` 파일을 받을 수 있나요?**  
A. 아닙니다. `.xlsx` 또는 `.xlsm`으로 다시 저장해 제출해야 합니다.

**Q. 별도 DB 서버를 운영해야 하나요?**  
A. 아닙니다. 현재는 JSON과 원본 Excel을 파일 기반 저장소로 사용합니다.

**Q. 자동 발송으로 바꾸면 기존 초안도 모두 나가나요?**  
A. 아닙니다. 새로 수집되어 검증 완료된 준비 건만 자동 처리 대상입니다. 다만 외부 발송이 즉시 일어날 수 있으므로 신중히 전환합니다.

**Q. 주소나 문구를 한 건만 바꾸려면 어떻게 하나요?**  
A. 포털의 초안 편집을 사용하고, 수신/참조·제목·본문·첨부를 다시 확인한 뒤 승인합니다.

## 18. 인수인계 체크리스트

- [ ] 감시 메일함과 업체 To/CC의 역할 차이를 설명할 수 있다.
- [ ] 제목의 `명함` 조건과 첨부 파일명 비사용 원칙을 이해한다.
- [ ] `.xlsx`/`.xlsm`만 지원하고 `.xls`는 제외됨을 안내할 수 있다.
- [ ] 현재 포털 주소, 기본 60초 간격, 최근 30건 확인 기준을 알고 있다.
- [ ] 직접 승인과 자동 발송의 차이와 현재 모드를 확인할 수 있다.
- [ ] 초안·대시보드·결과 JSON의 위치와 용도를 알고 있다.
- [ ] JSON, 해시, 원자적 저장, 작업 잠금, CSRF, 초안 digest의 실무 의미를 설명할 수 있다.
- [ ] 상태 손상 시 `recover_business_card_state.py` 복구 절차를 수행할 수 있다.
- [ ] `.env`, 신청서, 출력·백업 파일의 개인정보·인증정보 보호 원칙을 준수한다.
- [ ] 설정 변경 후 테스트·미리보기부터 하고, 문서 작업으로 메일 상태를 바꾸지 않는 원칙을 준수한다.
